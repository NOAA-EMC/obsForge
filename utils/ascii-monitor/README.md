ASCII Monitor scans observation files for selected cycles,
computes counts and historical averages, and generates ASCII reports.

Running the code:

module load obsforge/wcoss2.intel
python monitor.py config.yaml

See the script in examples/run_ascii_monitor.sh
and the config file in examples/config.yaml
